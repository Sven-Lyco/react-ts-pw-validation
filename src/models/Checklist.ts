export interface ChecklistProps {
  validLength: boolean;
  hasNumber: boolean;
  upperCase: boolean;
  lowerCase: boolean;
  specialChar: boolean;
  requiredLength: number;
  firstPasswordLength: number;
}
